# 蓝色光标：从名称到可核验收盘价

## 用户提示词

> 使用 `$a-share-research`，确认蓝色光标是哪只 A 股，并查询截至 2026-08-02 最近一个完整交易日的未复权收盘价。请说明来源是否一致、发行主体是否确认，以及结果有什么限制。

## Skill 执行

```text
<python> <skill-root>/scripts/entrypoint.py resolve --query 蓝色光标 --as-of 2026-08-02
<python> <skill-root>/scripts/entrypoint.py close --security SZSE:300058 --as-of 2026-08-02
```

第二条命令只使用第一条命令返回的规范证券标识；Skill 没有从 `300058` 的代码前缀猜测交易所。

## Agent 示例答复

蓝色光标被交叉核验为深交所 A 股 `SZSE:300058`。截至 `2026-08-02`，最近一个已完成交易日是 `2026-07-31`，未复权收盘价为 `14.350 CNY/share`，整体状态为 `limited`。

身份方面，深交所 A 股名录确认了证券代码、简称、上市状态和 `2010-02-26` 的上市日期；巨潮证券字典独立返回了相同代码和简称，但当前 `orgId=9900010147` 不声明交易所，也不足以独立验证发行主体关系，因此它只保留在来源证据中，不会进入规范候选的发行主体标识，发行主体关系明确标为 `unverified`。

价格方面，深交所日线返回 `14.35`，腾讯日线返回 `14.350`，交易日、证券身份、未复权口径和数值一致。结果仍是 `limited`，因为两个价格操作均属于尚未完成操作级资格审查的实验来源，首次公开时间也未完成独立核验。

## 关键证据

| 事实 | 来源操作 | 现场结果 |
| --- | --- | --- |
| 证券身份 | `szse_stock_list@1` | `SZSE:300058`，简称“蓝色光标” |
| 身份交叉核验 | `cninfo_security_dictionary@1` | 代码 `300058`，简称“蓝色光标”，opaque `orgId` |
| 交易会话与收盘价 | `szse_daily_line@1` | `2026-07-31` 已完成，`14.35 CNY/share` |
| 收盘价交叉核验 | `tencent_daily_line@1` | `2026-07-31`，`14.350 CNY/share` |

本案例是 `2026-08-02` 的现场快照，不代表阅读本文时的当前价格。
